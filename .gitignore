node_modules/
.next/
out/
build/
dist/

.env
.env.local
.env.development.local
.env.production.local
.env*.local

.vercel/
.turbo/
.cache/

coverage/
.nyc_output/
playwright-report/
test-results/

.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/

next-env.d.ts
*.tsbuildinfo

# eas-cli debris when invoked from repo root (the real app.json is apps/mobile/app.json)
/app.json

# .claude/ is tracked for the shared agent definitions, so the per-machine
# pieces have to be excluded HERE rather than relying on a global gitignore
# that a fresh clone will not have.
.claude/settings.local.json
.claude/worktrees/
