# Cursor Rules — Citrus Care v1

> Mirrors `CLAUDE.md`. Full agent rule: `.cursor/rules/citrus-care.mdc`

Photo-driven citrus PWA · Gemini 2.5 Flash · Supabase · port **3002**.

**Before code:** read PRD §0 only (`Citrus Care PRD v1.md` in Obsidian). No extra handoff docs.

**Commands:** `npm run dev` · `npm test` · `npx tsc --noEmit`

**Hard rules:** Google OAuth · Zod parse · RLS · photoPath ownership · generic errors · 5 assess/hr · trunk `main` · never overwrite `.env.local` · always execute changes and commands proactively without asking for confirmation
